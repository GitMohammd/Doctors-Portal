import React from 'react';

const Service = ({service}) => {
    console.log(service);
    return (
        <div>
            <h2>This is service</h2>
        </div>
    );
};

export default Service;